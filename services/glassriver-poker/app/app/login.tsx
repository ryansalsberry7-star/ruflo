import { FeatureScreen } from './components/FeatureScreen';

export default function LoginScreen() {
  return <FeatureScreen eyebrow="AUTH" title="Secure login" description="Authenticate with device-bound sessions, fraud checks, and account security controls." />;
}
