import { FeatureScreen } from '../components/FeatureScreen';

export default function ClubsScreen() {
  return <FeatureScreen eyebrow="PRIVATE CLUBS" title="Invite-only clubs" description="Create private poker communities with rankings, events, and moderated access." />;
}
